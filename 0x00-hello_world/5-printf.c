#include <stdio.h>
/***
 * this is the main function where the programme uses the function printf
 * it returns to 0 if successful
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art," / n);
	return (0);
}
