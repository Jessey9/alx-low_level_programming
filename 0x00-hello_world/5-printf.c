#include <stdio.h>
/***
 * main- C programme using the function printf and returns to 0 if successful
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art," / n);
	return (0);
}
