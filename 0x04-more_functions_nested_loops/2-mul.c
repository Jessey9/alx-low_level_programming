#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The multiplicand
 * @y: The multipler
 * Return: The result of multiplication between x and y paremeter
 */

int mul(int x, int y)
{
	return (x * y);
}
