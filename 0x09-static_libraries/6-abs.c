#include "main.h"
#include <stdio.h>

/**
 * absolute value - computes the absolute value of an integer
 * @c: The character to be checked
 * Return: Everytime 0
 */

int abs(int j)
{
	if (j < 0)
	{
		return -j;
	}
	else
	{
		return j;
	}
}
